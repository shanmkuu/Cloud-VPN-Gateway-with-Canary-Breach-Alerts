# Runtime dependencies
watchfiles>=0.21.0          # Async file watching (inotify-backed on Linux)
httpx[asyncio]>=0.27.0      # Async HTTP client for GeoIP & webhook calls
python-dotenv>=1.0.0        # .env file loading
rich>=13.7.0                # Terminal rich output (panels, tables, colors)

# Optional: Structured JSON logging
python-json-logger>=2.0.7

# Development / Testing dependencies (install with: pip install -r requirements-dev.txt)
# Listed here for CI/CD pip-audit scanning
# bandit[toml]>=1.7.8
# pytest>=8.1.0
# pytest-asyncio>=0.23.0
# pytest-mock>=3.12.0
# pip-audit>=2.7.3
